# Handoff: GLM-Ready Planning for Phases 7, 9, 10

**Created:** 2026-04-19
**Context:** User is low on Claude quota. Need to front-load architectural decisions so remaining phases can be executed by GLM-5.1.

---

## Current State

**Repository:** zarchon (archon-gsd)
**Branch:** main (up to date)

### Completed Phases
- 02: Task→Model Routing ✓
- 03: Escalation Detection + Failover ✓
- 03.1, 11, 11.1-11.5, 12, 12.1: All complete ✓

### Remaining Phases (Pre-Backlog)
| Phase | Title | GLM-Ready? |
|-------|-------|------------|
| 01 | Model Registry + Config Schema | ✅ Yes |
| 04 | z.ai + Ollama Integration | ✅ Yes |
| 05 | Cross-Model Review Workflow | ✅ Yes |
| 06 | Phase Conventions | ✅ Yes |
| **07** | **Claudeception Feedback Loop** | ⚠️ Needs planning |
| 08 | Local-Only Mode | ✅ Yes |
| **09** | **Autonomous DEBT Processing** | ⚠️ Needs planning |
| **10** | **Autonomous Issue Processing** | ⚠️ Needs planning |

---

## Goal of This Session

Run `/gsd-discuss-phase` and `/gsd-plan-phase` for phases **7, 9, 10** to:
1. Lock architectural decisions in CONTEXT.md
2. Research pitfalls in RESEARCH.md
3. Create detailed PLAN.md with explicit tasks

This front-loads judgment work (Claude) so execution work (GLM) becomes pattern-following.

---

## Phase 7: Claudeception Feedback Loop

**Intent:** Log routing decisions and outcomes so the system learns which models succeed at which tasks.

**Key decisions needed:**
- Learning algorithm: How to weight recent vs old outcomes?
- Persistence: Where does learned state live?
- Integration: How do learnings affect routing?

**Existing infrastructure:**
- `logging.sh`: Already logs routing decisions and task outcomes to delegation-log.jsonl
- `routing.sh`: Has `_compute_weighted_failure_rate()` that could consume learnings

---

## Phase 9: Autonomous DEBT Processing

**Intent:** Use cheap async models to autonomously work DEBT items to a reviewable state.

**Key decisions needed:**
- State machine: IDLE → PICKING → IMPLEMENTING → PR → REVIEWING → DONE
- Recovery: What happens when a step fails?
- Integration: How to invoke ai-delegate for implementation?

**Existing infrastructure:**
- `ai-delegate impl`: Can implement features
- `review-pr` skill: Can delegate review to Codex
- Phase conventions in config.json: DEBT prefix defined

---

## Phase 10: Autonomous Issue Processing

**Intent:** Triage GitHub issues into WANT.x planning artifacts using cheap models.

**Key decisions needed:**
- WANT.x artifact format (exact template)
- Triage decision tree (complexity scoring)
- Label mapping (keywords → labels)

**Existing infrastructure:**
- `gh issue list` / `gh issue view`: GitHub CLI
- Phase conventions in config.json: WANT prefix defined
- Complexity scoring exists in routing.sh (could adapt)

---

## Next Steps

```bash
# Start with Phase 7
/gsd-discuss-phase 7

# Then plan it
/gsd-plan-phase 7

# Repeat for 9 and 10
/gsd-discuss-phase 9
/gsd-plan-phase 9

/gsd-discuss-phase 10
/gsd-plan-phase 10
```

After all three are planned, phases 01-10 can all be executed with:
```bash
/gsd-set-profile budget
/gsd-execute-phase <N>
```

---

## Key Files

- `.planning/ROADMAP.md` — Phase definitions
- `~/dev/.meta/bin/lib/logging.sh` — Existing logging (for Phase 7)
- `~/dev/.meta/bin/lib/routing.sh` — Existing routing (for Phase 7)
- `.planning/config.json` — Phase conventions, model config
