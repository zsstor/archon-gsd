---
phase: 11.4-code-review-fixes
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - ~/dev/.meta/bin/lib/test_routing.sh
  - ~/dev/.meta/bin/lib/test_delegate.sh
autonomous: true
requirements:
  - CR-01
  - CR-02
  - WR-01
  - WR-02
  - WR-03
  - WR-04
  - WR-05
  - WR-06
  - WR-07
  - WR-08

must_haves:
  truths:
    - "Test suites exist before any production code changes"
    - "Each finding has at least one test case covering the fix"
    - "Tests initially fail on unfixed code (TDD red phase)"
  artifacts:
    - path: "~/dev/.meta/bin/lib/test_routing.sh"
      provides: "Test suite for routing.sh security fixes"
      contains: "test_jq_injection_prevention"
    - path: "~/dev/.meta/bin/lib/test_delegate.sh"
      provides: "Test suite for ai-delegate security fixes"
      contains: "test_profile_source_opt_in"
  key_links:
    - from: "test_routing.sh"
      to: "routing.sh"
      via: "source statement at top"
      pattern: "source.*routing\\.sh"
    - from: "test_delegate.sh"
      to: "ai-delegate"
      via: "function calls"
      pattern: "source.*ai-delegate"
---

<objective>
Create test suites for Phase 11.4 code review fixes before modifying production code.

Purpose: Enable TDD workflow - tests must exist and fail before fixes are applied. This ensures each fix is verifiable and prevents regressions.

Output: Two executable test files (`test_routing.sh`, `test_delegate.sh`) following existing test patterns from `test_quota.sh` and `test_escalation.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11.4-code-review-fixes/11.4-RESEARCH.md
@.planning/phases/11.4-code-review-fixes/11.4-REVIEW.md

<interfaces>
<!-- Existing test helper patterns from test_quota.sh -->
From ~/dev/.meta/bin/lib/test_quota.sh:
```bash
assert_exit_code() {
    local expected="$1"
    local actual="$2"
    local test_name="$3"
    # ... comparison logic
}

assert_output_contains() {
    local output="$1"
    local expected="$2"
    local test_name="$3"
    # ... comparison logic
}

assert_output_equals() {
    local actual="$1"
    local expected="$2"
    local test_name="$3"
    # ... comparison logic
}

assert_file_exists() {
    local file_path="$1"
    local test_name="$2"
    # ... comparison logic
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_routing.sh test suite</name>
  <files>~/dev/.meta/bin/lib/test_routing.sh</files>
  <read_first>
    - ~/dev/.meta/bin/lib/test_quota.sh (reference implementation for test patterns)
    - ~/dev/.meta/bin/lib/routing.sh (module being tested)
    - .planning/phases/11.4-code-review-fixes/11.4-REVIEW.md (findings to test)
  </read_first>
  <behavior>
    - test_jq_injection_prevention: Call _get_scoring_mode with task_type containing special chars ("; rm -rf /", "'; cat /etc/passwd", '$("whoami")') — should return "keyword" (default) without executing injected code
    - test_model_selection_bounds_check: Call _select_model with complexity_score=70 but only 2-element model array — should return models[0] or fallback, not empty string
    - test_model_selection_bounds_single: Call _select_model with complexity_score=80 and 1-element model array — should return that single model
    - test_atomic_config_write: Call _upgrade_scoring_mode, then interrupt with SIGTERM during write — config should remain valid JSON
    - test_float_comparison_awk_fallback: Set PATH without python3, call _check_upgrade_trigger — should use awk and return correctly
    - test_datetime_parse_warning: Pass malformed timestamp to _compute_weighted_failure_rate — should log warning to stderr
    - test_complexity_constants_defined: Check that COMPLEXITY_THRESHOLD_LOW and COMPLEXITY_THRESHOLD_HIGH are readonly
  </behavior>
  <action>
Create ~/dev/.meta/bin/lib/test_routing.sh following test_quota.sh structure:
1. Shebang: #!/usr/bin/env bash
2. Comment header: "Test suite for routing.sh fixes (Phase 11.4)"
3. Requirements comment: "CR-01, WR-01, WR-02, WR-03, WR-07, IN-02, IN-04"
4. set -uo pipefail (NOT set -e, so tests can check failures)
5. Source routing.sh with mock CONFIG_FILE and DELEGATION_LOG pointing to TEST_DIR
6. Copy test helper functions (assert_exit_code, assert_output_contains, assert_output_equals, assert_file_exists) from test_quota.sh
7. Add these specific test functions:
   - test_jq_injection_prevention(): Create task_type with injection payload, verify _get_scoring_mode returns "keyword" without side effects
   - test_model_selection_bounds_check(): Mock _get_model_chain to return 2-element array, call _select_model with score=70, verify it returns valid model
   - test_model_selection_bounds_single(): Mock array with 1 element, score=80, verify returns that element
   - test_atomic_config_write(): Create config, call _upgrade_scoring_mode in subshell, kill -15, verify JSON still valid
   - test_float_comparison_awk_fallback(): Temporarily rename python3, verify _check_upgrade_trigger still works
   - test_datetime_parse_warning(): Create log with invalid timestamp, capture stderr, verify "Warning" appears
   - test_complexity_constants_defined(): Check readonly status of threshold constants
8. Main test runner that calls each test and reports summary
9. Make executable: chmod +x
  </action>
  <verify>
    <automated>bash ~/dev/.meta/bin/lib/test_routing.sh 2>&1 | grep -E "^(PASS|FAIL|SKIP|Tests:)"</automated>
  </verify>
  <acceptance_criteria>
    - File ~/dev/.meta/bin/lib/test_routing.sh exists with 644+ permissions
    - File contains "test_jq_injection_prevention" function definition
    - File contains "test_model_selection_bounds_check" function definition
    - File contains "test_atomic_config_write" function definition
    - File contains "test_float_comparison_awk_fallback" function definition
    - File contains "test_datetime_parse_warning" function definition
    - File contains "source.*routing\.sh"
    - Running test_routing.sh exits with code 0 or 1 (runs without syntax errors)
    - Output contains "Tests:" summary line
  </acceptance_criteria>
  <done>Test suite for routing.sh fixes created. Tests may fail (expected - production code not yet fixed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_delegate.sh test suite</name>
  <files>~/dev/.meta/bin/lib/test_delegate.sh</files>
  <read_first>
    - ~/dev/.meta/bin/lib/test_quota.sh (reference implementation for test patterns)
    - ~/dev/.meta/bin/ai-delegate (script being tested)
    - .planning/phases/11.4-code-review-fixes/11.4-REVIEW.md (findings to test)
  </read_first>
  <behavior>
    - test_profile_source_opt_in: Run ai-delegate with ~/.profile containing echo marker, AI_DELEGATE_SOURCE_PROFILE unset — marker should NOT appear in env/output
    - test_profile_source_enabled: Run ai-delegate with AI_DELEGATE_SOURCE_PROFILE=true — profile should be sourced
    - test_module_existence_check: Remove routing.sh temporarily, run ai-delegate — should fail with "Required module not found: routing.sh"
    - test_review_task_type_consistency: Mock _get_model_chain, call cmd_review, verify it passes "review" (not "code-review") to _get_model_chain
    - test_context_file_readable: Create context file with no read permissions, run cmd_impl — should log warning, not crash
    - test_prompt_size_limit: Create reference directory with huge files (100KB each), run cmd_scaffold — prompt should be truncated
    - test_die_includes_location: Call die function, capture stderr — should contain filename and line number
    - test_verbose_routing_helper: Call with VERBOSE=true, verify output format consistency
    - test_session_cleanup_comment: Check _cleanup_session has explanatory comment above it
  </behavior>
  <action>
Create ~/dev/.meta/bin/lib/test_delegate.sh following test_quota.sh structure:
1. Shebang: #!/usr/bin/env bash
2. Comment header: "Test suite for ai-delegate fixes (Phase 11.4)"
3. Requirements comment: "CR-02, WR-04, WR-05, WR-06, WR-08, IN-01, IN-03, IN-05"
4. set -uo pipefail
5. Setup: Create TEST_DIR with mock config, profile, modules
6. Copy test helper functions from test_quota.sh
7. Add these specific test functions:
   - test_profile_source_opt_in(): Create ~/.profile.test with export MARKER=set, symlink or env, run ai-delegate help, verify MARKER not set
   - test_profile_source_enabled(): Set AI_DELEGATE_SOURCE_PROFILE=true, verify profile is sourced
   - test_module_existence_check(): Backup and remove routing.sh, run ai-delegate, verify error message contains "Required module not found"
   - test_review_task_type_consistency(): Verify cmd_review uses consistent task_type string
   - test_context_file_readable(): Create unreadable file, run cmd_impl with it, verify warning logged (not crash)
   - test_prompt_size_limit(): Create 100KB file in ref:path, verify prompt is truncated (< 15KB)
   - test_die_includes_location(): Source ai-delegate functions, call die, verify stderr contains line number pattern
   - test_verbose_routing_helper(): Verify verbose_routing function exists and outputs consistently
   - test_session_cleanup_comment(): grep for comment above _cleanup_session
8. Main test runner with summary
9. Make executable: chmod +x
  </action>
  <verify>
    <automated>bash ~/dev/.meta/bin/lib/test_delegate.sh 2>&1 | grep -E "^(PASS|FAIL|SKIP|Tests:)"</automated>
  </verify>
  <acceptance_criteria>
    - File ~/dev/.meta/bin/lib/test_delegate.sh exists with 644+ permissions
    - File contains "test_profile_source_opt_in" function definition
    - File contains "test_module_existence_check" function definition
    - File contains "test_context_file_readable" function definition
    - File contains "test_prompt_size_limit" function definition
    - File contains "test_die_includes_location" function definition
    - Running test_delegate.sh exits with code 0 or 1 (runs without syntax errors)
    - Output contains "Tests:" summary line
  </acceptance_criteria>
  <done>Test suite for ai-delegate fixes created. Tests may fail (expected - production code not yet fixed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures | Test data files created in TEST_DIR could be adversarial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.4-00-01 | Tampering | test fixtures | accept | Tests use isolated TEST_DIR with cleanup trap, no production data |
| T-11.4-00-02 | Denial | test isolation | mitigate | Each test uses unique temp dirs, cleanup on exit |
</threat_model>

<verification>
```bash
# Verify both test files exist and are executable
ls -la ~/dev/.meta/bin/lib/test_routing.sh ~/dev/.meta/bin/lib/test_delegate.sh

# Verify test_routing.sh runs (some failures expected)
bash ~/dev/.meta/bin/lib/test_routing.sh 2>&1 | tail -5

# Verify test_delegate.sh runs (some failures expected)
bash ~/dev/.meta/bin/lib/test_delegate.sh 2>&1 | tail -5

# Verify required test functions exist
grep -c "^test_" ~/dev/.meta/bin/lib/test_routing.sh
grep -c "^test_" ~/dev/.meta/bin/lib/test_delegate.sh
```
</verification>

<success_criteria>
- Both test files created with correct structure
- Test files source their target modules
- Each finding (CR-01, CR-02, WR-01-08) has at least one test function
- Tests run without syntax errors (may report failures)
- Test summary line printed at end
</success_criteria>

<output>
After completion, create `.planning/phases/11.4-code-review-fixes/11.4-00-SUMMARY.md`
</output>
